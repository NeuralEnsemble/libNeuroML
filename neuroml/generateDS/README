Autogeneration of API, using genreateds_config.py to ensure correct naming conventions.

generateDS.py should be invoked in this folder (so that generateds_config.py can be located) with the following command:

   $ generateDS.py -o nml.py -s nmlsubs.py NeuroML_v2alpha.xsd